Static-site host for `dev`. Each subfolder is served at the
corresponding URL path on that domain.

`/dash` is a retro terminal-style dashboard of the active projects
(v0.1.0) — it reads each project's live version at load, so its cards
stay current without manual edits.
